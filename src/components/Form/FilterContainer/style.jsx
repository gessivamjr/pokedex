import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    filter__container: {
        alignItems: "center",
        width: "100%",
    },
    filter__label: {
        fontSize: 22,
    },
});

export default styles;
